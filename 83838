<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minimalist Project</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Roboto:wght@400&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #FFFFFF;
            --secondary-bg: #F5F5F5;
            --text-color: #212121;
            --accent-color: #1A73E8;
            --border-color: #E0E0E0;
            --container-width: 1200px;
            --transition: all 0.3s ease;
        }

        [data-theme="dark"] {
            --bg-color: #121212;
            --secondary-bg: #1E1E1E;
            --text-color: #F5F5F5;
            --border-color: #333333;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            transition: var(--transition);
            scroll-behavior: smooth;
        }

        h1, h2, h3 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            line-height: 1.2;
        }

        h1 { font-size: 48px; margin-bottom: 24px; }
        h2 { font-size: 32px; margin-bottom: 48px; text-align: center; }

        .container {
            max-width: var(--container-width);
            margin: 0 auto;
            padding: 0 24px;
        }

        section {
            padding: 96px 0;
        }

        /* Навигация */
        nav {
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid var(--border-color);
            position: sticky;
            top: 0;
            background: var(--bg-color);
            z-index: 1000;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text-color);
            margin-left: 32px;
            font-weight: 500;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .theme-toggle {
            cursor: pointer;
            background: none;
            border: 1px solid var(--border-color);
            padding: 8px 16px;
            color: var(--text-color);
            border-radius: 4px;
        }

        /* Hero блок */
        .hero {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            min-height: 80vh;
            background-color: var(--secondary-bg);
        }

        .cta-button {
            background-color: var(--accent-color);
            color: white;
            padding: 16px 32px;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            display: inline-block;
            transition: opacity 0.2s;
        }

        .cta-button:hover { opacity: 0.9; }

        /* Сетка услуг */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 32px;
        }

        .card {
            padding: 32px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            transition: var(--transition);
        }

        .card:hover {
            border-color: var(--accent-color);
            transform: translateY(-4px);
        }

        /* Форма */
        .contact-form {
            max-width: 600px;
            margin: 0 auto;
        }

        input, textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 16px;
            border: 1px solid var(--border-color);
            background: var(--bg-color);
            color: var(--text-color);
            border-radius: 4px;
        }

        /* Футер */
        footer {
            padding: 48px 0;
            border-top: 1px solid var(--border-color);
            text-align: center;
            font-size: 14px;
        }

        /* Адаптивность */
        @media (max-width: 768px) {
            h1 { font-size: 32px; }
            section { padding: 64px 0; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

<div class="container">
    <nav>
        <div style="font-weight: 700; font-size: 20px;">LOGOTYPE</div>
        <div class="nav-links">
            <a href="#about">О нас</a>
            <a href="#services">Услуги</a>
            <a href="#portfolio">Портфолио</a>
            <a href="#contacts">Контакты</a>
        </div>
        <button class="theme-toggle" onclick="toggleTheme()">Тема</button>
    </nav>
</div>

<section class="hero">
    <div class="container">
        <h1>Лаконичные решения для вашего бизнеса</h1>
        <p style="margin-bottom: 32px; max-width: 600px; margin-left: auto; margin-right: auto;">
            Создаем функциональные сайты с акцентом на типографику и пространство. Ничего лишнего, только результат.
        </p>
        <a href="#contacts" class="cta-button">Начать проект</a>
    </div>
</section>

<section id="about">
    <div class="container">
        <h2>Почему мы?</h2>
        <div class="grid">
            <div class="card">
                <h3>Скорость</h3>
                <p>Загрузка страниц менее 1.5 секунд благодаря оптимизации кода.</p>
            </div>
            <div class="card">
                <h3>Дизайн</h3>
                <p>Чистый визуальный стиль, который не отвлекает от главного.</p>
            </div>
            <div class="card">
                <h3>Конверсия</h3>
                <p>Интуитивная навигация ведет пользователя прямо к цели.</p>
            </div>
        </div>
    </div>
</section>

<section id="services" style="background-color: var(--secondary-bg);">
    <div class="container">
        <h2>Наши услуги</h2>
        <div class="grid">
            <div class="card">
                <div style="font-size: 24px; margin-bottom: 16px;">🌐</div>
                <h3>Веб-разработка</h3>
                <p>Создание адаптивных сайтов любой сложности.</p>
            </div>
            <div class="card">
                <div style="font-size: 24px; margin-bottom: 16px;">🎨</div>
                <h3>UI/UX Дизайн</h3>
                <p>Проектирование интерфейсов, удобных для людей.</p>
            </div>
            <div class="card">
                <div style="font-size: 24px; margin-bottom: 16px;">📈</div>
                <h3>SEO Оптимизация</h3>
                <p>Вывод вашего продукта в топ поисковой выдачи.</p>
            </div>
        </div>
    </div>
</section>

<section id="contacts">
    <div class="container">
        <h2>Связаться с нами</h2>
        <form class="contact-form" onsubmit="event.preventDefault(); alert('Сообщение отправлено!');">
            <input type="text" placeholder="Ваше имя" required>
            <input type="email" placeholder="Email" required>
            <textarea rows="5" placeholder="Ваше сообщение"></textarea>
            <button type="submit" class="cta-button" style="border: none; width: 100%; cursor: pointer;">Отправить</button>
        </form>
    </div>
</section>

<footer>
    <div class="container">
        <p>&copy; 2026 Minimalist Agency. Все права защищены.</p>
        <div style="margin-top: 16px;">
            <a href="#" style="color: var(--text-color); margin: 0 10px;">Privacy Policy</a>
            <a href="#" style="color: var(--text-color); margin: 0 10px;">Terms of Service</a>
        </div>
    </div>
</footer>

<script>
    function toggleTheme() {
        const body = document.body;
        const currentTheme = body.getAttribute('data-theme');
        if (currentTheme === 'dark') {
            body.removeAttribute('data-theme');
        } else {
            body.setAttribute('data-theme', 'dark');
        }
    }

    // Плавное появление блоков при скролле
    const observerOptions = { threshold: 0.1 };
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);

    document.querySelectorAll('.card').forEach(card => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(20px)';
        card.style.transition = 'all 0.6s ease-out';
        observer.observe(card);
    });
</script>

</body>
</html>
